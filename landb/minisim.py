def gen_node(sentence: str):
    """
    Takes a sentence and extracts the words representing objects and their relations.
    These objects are put into a multi-directed graph (a graph allowing multiple arrows between objects).
    :param sentence:
    :return:
    """